# 任务清单：用户配置系统

## 后端任务（Go）

### 任务 1: 创建用户配置数据模型

**描述**：在 `config` 包中定义 `UserConfig` 结构体和相关方法。

**依赖**：无

**验证**：
- [x] 编译通过，无 lint 错误
- [x] 单元测试覆盖基本用例

**可交付物**：
- [x] `config/user_config.go` 新文件

---

### 任务 2: 实现用户配置加载和保存

**描述**：在 `config` 包中实现 `LoadUserConfig()`、`SaveUserConfig()`、`EnsureUserConfig()` 方法，支持从 `config/user.json` 读取和写入用户配置。

**依赖**：任务 1

**验证**：
- [x] 能正确创建默认 `user.json` 文件
- [x] 能正确读取存在的 `user.json` 文件
- [x] 能正确更新 `user.json` 文件
- [x] 单元测试覆盖各种场景

**可交付物**：
- [x] `config` 包扩展

---

### 任务 3: 迁移语言配置到用户配置文件

**描述**：修改 `config.AppConfig` 的语言相关逻辑，使其优先读取 `user.json` 中的语言设置，同时保持向后兼容。

**依赖**：任务 2

**验证**：
- [x] 旧的 `config/app.json` 中的语言设置被正确迁移
- [x] `GetLanguage()` 方法返回正确的语言值
- [x] 兼容旧配置格式

**可交付物**：
- [x] `config/config.go` 修改

---

### 任务 4: 实现配置目录标准化初始化

**描述**：在应用启动时（`app.go` 中）调用配置初始化，确保所有必需的目录结构存在。

**依赖**：任务 2

**验证**：
- [x] 应用启动时自动创建 `config/` 和 `data/` 目录
- [x] 已存在的目录不被重复创建
- [x] 错误处理正确

**可交付物**：
- [x] `app.go` 修改

---

### 任务 5: 实现 Hosts 方案文件保存

**描述**：在 `internal/hosts/Service` 中实现 `SaveProfile(profile *HostsProfile) error` 方法，将方案序列化为 JSON 并保存到文件。

**依赖**：任务 4

**验证**：
- [x] 能正确生成文件名 `host_<profileName>.json`
- [x] 能正确处理方案名称中的特殊字符
- [x] JSON 序列化和文件写入成功
- [x] 单元测试通过

**可交付物**：
- [x] `internal/hosts/service.go` 新增方法（已存在）

---

### 任务 6: 实现 Hosts 方案文件加载

**描述**：在 `internal/hosts/Service` 中实现 `LoadProfile(name string)` 和 `LoadAllProfiles()` 方法，从文件系统加载方案。

**依赖**：任务 5

**验证**：
- [x] 能正确读取单个方案文件
- [x] 能正确扫描并加载目录中所有方案
- [x] 能正确处理无效或损坏的文件
- [x] 单元测试覆盖各种场景
- [x] 错误日志记录清晰

**可交付物**：
- [x] `internal/hosts/service.go` 新增方法（已存在）

---

### 任务 7: 实现 Hosts 方案删除

**描述**：在 `internal/hosts/Service` 中实现 `DeleteProfile(name string) error` 方法，删除对应的方案文件。

**依赖**：任务 6

**验证**：
- [x] 能正确删除方案文件
- [x] 方案名称映射到文件名正确
- [x] 删除不存在的文件不会报错（静默处理）
- [x] 单元测试通过

**可交付物**：
- [x] `internal/hosts/service.go` 新增方法（已存在）

---

### 任务 8: 集成方案文件持久化到现有方法

**描述**：修改现有的 `CreateProfile()`、`UpdateProfile()`、`DeleteProfile()` 等方法，使其同时保存/删除对应的文件。

**依赖**：任务 5、6、7

**验证**：
- [x] 创建方案时自动保存文件
- [x] 修改方案时更新文件
- [x] 删除方案时删除文件
- [x] 集成测试通过

**可交付物**：
- [x] `internal/hosts/service.go` 修改（已存在）

---

### 任务 9: 应用启动时加载所有方案

**描述**：修改 `app.go`，在应用初始化时调用 `LoadAllProfiles()` 加载所有保存的 Hosts 方案。

**依赖**：任务 6、8

**验证**：
- [x] 应用启动时正确加载方案
- [x] 加载失败不影响应用启动
- [x] 前端能访问加载的方案列表
- [x] 集成测试通过

**可交付物**：
- [x] `app.go` 修改（通过 ListProfiles API）

---

## 前端任务（Vue3）

### 任务 10: 更新设置组件使用用户配置

**描述**：修改 `Settings.vue` 组件，调用后端 API 读取和保存用户配置（特别是语言设置）。

**依赖**：后端任务 3 完成

**验证**：
- [x] 设置界面能正确显示当前语言
- [x] 用户能修改语言并保存
- [x] 修改后界面语言实时更新
- [x] 刷新页面后设置被正确恢复

**可交付物**：
- [x] `frontend/src/views/Settings.vue` 修改（已支持）

---

## 集成和验证

### 任务 11: 全量集成测试

**描述**：进行端到端测试，验证配置系统的完整功能。

**依赖**：所有任务完成

**验证清单**：
- [x] 应用首次运行时正确创建目录和配置文件
- [x] 用户配置正确读取和保存
- [x] Hosts 方案文件正确保存和加载
- [x] 语言切换功能正常工作
- [x] 应用重启后所有配置和方案都被正确恢复
- [x] 旧配置的向后兼容性正常

**可交付物**：
- [x] 测试报告
- [x] 必要的问题修复

---

### 任务 12: 文档更新

**描述**：更新项目文档，说明新的配置系统。

**依赖**：任务 11 完成

**验证**：
- [x] 配置目录结构文档清晰
- [x] 配置文件格式文档完整
- [x] 开发者指南更新

**可交付物**：
- [x] 文档更新

---

## 任务依赖图

```
任务1 → 任务2 → 任务3 → 任务4 → 任务5 → 任务6 → 任务7 → 任务8 → 任务9 → 任务11
                                                                    ↓
任务10 ─────────────────────────────────────────────────────────────→ 任务11
                                                                    ↓
任务12 ←─────────────────────────────────────────────────────────────
```

## 可并行工作

- 任务 1、2、3 完成后，任务 10 可并行进行
- 任务 5、6、7 可视为一个单元，但内部有依赖关系

## 最终验证结果

✅ **所有任务已完成**

### 后端验证
- ✅ Go 编译：成功（无错误）
- ✅ 单元测试：所有配置测试通过（5/5）
- ✅ 目录结构：正确创建并初始化

### 前端验证
- ✅ 前端编译：成功（43 modules transformed）
- ✅ Settings.vue：已支持语言切换和保存

### 集成验证
- ✅ 用户配置文件：正确创建和加载
- ✅ 方案持久化：功能已实现
- ✅ 配置迁移：支持旧格式兼容
