# 任务清单：国际化支持实施

## 总体说明

此变更包含两个主要阶段：
1. **i18n-core**：建立国际化基础设施（任�?1.1 - 1.7�?
2. **i18n-settings**：添加设置界面（任务 2.1 - 2.4�?

### 任务依赖关系

```
1.1 �?1.2 �?1.3 �?1.4 �?1.5 �?1.6 �?1.7
                                    �?
                              2.1 �?2.2 �?2.3 �?2.4
```

---

## 阶段 1：i18n-core（国际化基础设施�?

### 任务 1.1：添�?vue-i18n 依赖
**目标**：在项目中集�?vue-i18n �?

**具体步骤**�?
1. �?`frontend/package.json` 中添加依赖：`vue-i18n@^9.0.0`
2. 运行 `npm install`
3. 验证 `node_modules` �?vue-i18n 已安�?

**验收标准**�?
- [x] vue-i18n �?package.json 中正确声�?
- [x] npm install 成功执行
- [x] node_modules 中包�?vue-i18n

**工作�?*�?.5 小时

---

### 任务 1.2：创建翻译资源文件结�?
**目标**：建�?i18n 目录和翻译文�?

**具体步骤**�?
1. 创建目录：`frontend/src/i18n/locales/`
2. 创建文件：`frontend/src/i18n/locales/zh-CN.json`
3. 创建文件：`frontend/src/i18n/locales/en-US.json`
4. 创建文件：`frontend/src/i18n/index.js`

**输入**�?
- 设计文档中定义的翻译资源结构和内�?

**输出**�?
- 完整的中英文翻译资源文件
- i18n 初始化配置文�?

**验收标准**�?
- [x] 目录结构正确创建
- [x] zh-CN.json 包含所有中文翻�?
- [x] en-US.json 包含所有英文翻�?
- [x] 翻译文件格式有效（JSON�?

**工作�?*�? 小时

---

### 任务 1.3：创�?I18n 初始化模�?
**目标**：实�?i18n 的初始化和语言管理逻辑

**具体步骤**�?
1. 完善 `frontend/src/i18n/index.js`
2. 实现 `initI18n()` 函数
3. 实现 `setLanguage()` 函数
4. 实现 `getLanguage()` 函数
5. 导出需要的函数�?i18n 实例

**代码要求**�?
- 使用 Composition API 风格
- 支持 legacy: false 配置
- 默认语言�?zh-CN
- �?localStorage 读取语言偏好

**验收标准**�?
- [x] initI18n() 正确初始�?i18n 实例
- [x] setLanguage() 正确保存语言�?localStorage
- [x] getLanguage() 正确读取语言偏好
- [x] 初始化逻辑可在 main.js 中集�?

**工作�?*�?.5 小时

---

### 任务 1.4：集�?i18n �?main.js
**目标**：在应用启动时初始化 i18n

**具体步骤**�?
1. �?`frontend/src/main.js` 中导�?initI18n
2. 调用 initI18n() 获取 i18n 实例
3. �?i18n 插件添加�?Vue app
4. 验证应用正常启动

**验收标准**�?
- [x] main.js 成功导入并初始化 i18n
- [x] Vue app 正确使用 i18n 插件
- [x] 开发服务器启动不报�?

**工作�?*�?.5 小时

---

### 任务 1.5：创�?Go 语言检测模�?
**目标**：实�?Windows 系统语言检测功�?

**具体步骤**�?
1. 创建 `internal/language/` 目录
2. 创建 `internal/language/detector.go` 文件
3. 实现 `GetSystemLanguage()` 函数
   - 使用 Windows API 获取用户区域设置
   - 返回格式�?zh-CN、en-US �?
   - 如果获取失败，返回默认�?zh-CN
4. 添加单元测试

**技术细�?*�?
- 使用 `syscall` 包调�?Windows API
- LOCALE_SNAME 常量用于获取语言代码
- 错误处理必须稳健

**验收标准**�?
- [x] GetSystemLanguage() 实现正确
- [x] 能正确识别中英文系统
- [x] 异常处理完善
- [x] 单元测试覆盖主要场景

**工作�?*�? 小时

---

### 任务 1.6：扩展应用配置支持语言设置
**目标**：在配置系统中添加语言字段和方�?

**具体步骤**�?
1. 打开 `config/config.go`
2. �?`AppConfig` 结构体中添加 `Language string` 字段
3. 添加 `SaveLanguage(lang string) error` 方法
4. 添加 `GetLanguage() string` 方法
   - 实现优先级逻辑：用户设�?> 系统语言 > 默认中文
5. 更新配置序列�?反序列化逻辑

**验收标准**�?
- [x] AppConfig 结构体包�?Language 字段
- [x] SaveLanguage() 正确保存语言偏好
- [x] GetLanguage() 实现正确的优先级
- [x] 配置文件正确读写

**工作�?*�?.5 小时

---

### 任务 1.7：暴�?Wails 绑定接口
**目标**：在 app.go 中添加语言相关的后端接�?

**具体步骤**�?
1. 打开 `app.go`
2. 添加 `GetSystemLanguage()` 方法
   - 调用 language.GetSystemLanguage()
   - 返回系统语言代码
3. 添加 `GetUserLanguagePreference()` 方法
   - 返回用户保存的语言偏好
4. 添加 `SetUserLanguagePreference(lang string)` 方法
   - 验证语言值有�?
   - 调用 a.cfg.SaveLanguage()
   - 返回错误（如有）
5. �?startup() 中添加初始化逻辑

**验收标准**�?
- [x] 三个接口正确实现并暴�?
- [x] 接口能从前端通过 Wails 调用
- [x] 错误处理完善
- [x] 应用启动正常

**工作�?*�? 小时

---

## 阶段 2：i18n-settings（设置界面）

### 任务 2.1：迁移现有组件使�?i18n
**目标**：更新现有组件，使用 i18n 进行文本国际�?

**具体步骤**�?
1. 打开 `frontend/src/views/HostsManager.vue`
2. 导入 `useI18n` 钩子
3. �?`$t()` 替换所有硬编码的中文文�?
   - 菜单项标�?
   - 按钮标签
   - 表格列标�?
   - 提示信息
   - 占位符文�?
4. 打开 `frontend/src/components/Sidebar.vue`
5. �?`$t()` 替换菜单项标签和其他文本
6. 验证应用仍正常运�?

**变更范围**�?
- HostsManager.vue（所有文本）
- Sidebar.vue（菜单标签）
- 其他已有组件的文�?

**验收标准**�?
- [x] HostsManager.vue 使用 i18n
- [x] Sidebar.vue 使用 i18n
- [x] 所有硬编码文本已替�?
- [x] 应用运行无错�?
- [x] 界面显示正确

**工作�?*�? 小时

---

### 任务 2.2：创�?Settings 视图
**目标**：创建设置界面，包含语言选择功能

**具体步骤**�?
1. 创建 `frontend/src/views/Settings.vue` 文件
2. 实现组件结构�?
   - 标题：使�?i18n
   - 语言选择下拉菜单
   - 当前语言显示
   - 成功提示区域
3. 实现选项数据�?
   ```javascript
   const languages = [
     { value: 'zh-CN', label: '中文 (Chinese)' },
     { value: 'en-US', label: 'English' }
   ]
   ```
4. 导入 Wails 后端接口

**UI 要求**�?
- 使用 Tailwind CSS 样式
- 符合现有设计风格
- 响应式布局
- 清晰的标签和选项

**验收标准**�?
- [x] Settings.vue 组件创建成功
- [x] UI 界面清晰美观
- [x] 语言选择菜单显示正确
- [x] 组件可以正确导入和使�?

**工作�?*�? 小时

---

### 任务 2.3：实现语言切换逻辑
**目标**：完成语言选择和实时切换功�?

**具体步骤**�?
1. �?Settings.vue 中实�?`changeLanguage()` 方法
2. 调用后端 `SetUserLanguagePreference()`
3. 更新 i18n 语言：`locale.value = selectedLanguage.value`
4. 保存�?localStorage
5. 显示成功提示信息
6. 验证所有界面文本实时更�?

**逻辑流程**�?
```
用户选择语言 �?调用后端保存 �?更新 localStorage �?切换 i18n 语言 �?刷新界面 �?显示提示
```

**验收标准**�?
- [x] 用户选择语言后立即切�?
- [x] 后端语言设置被正确保�?
- [x] localStorage 被正确更�?
- [x] 所有界面文本动态更�?
- [x] 成功提示正确显示
- [x] 无控制台错误

**工作�?*�?.5 小时

---

### 任务 2.4：集�?Settings 到菜单导�?
**目标**：确保用户能从菜单访问设�?

**具体步骤**�?
1. 验证 Sidebar.vue �?设置"菜单项已存在
2. 验证路由配置支持 settings 路由
3. 在主视图（如 App.vue）中添加路由处理
4. 连接 Settings.vue 到路�?
5. 验证点击菜单可以打开设置页面
6. 验证从设置页面返回正�?

**验收标准**�?
- [x] 菜单"设置"项可点击
- [x] 点击后成功打开 Settings 页面
- [x] Settings 页面中语言切换可正常使�?
- [x] 返回菜单页面正常
- [x] 导航流程平顺无错�?

**工作�?*�? 小时

---

## 集成测试（任�?2.5�?

### 验收测试
**目标**：验证整个国际化功能的完整�?

**测试场景**�?
1. 应用启动时自动检测系统语言
2. 第一次启动：显示系统语言（或默认中文�?
3. 打开设置，选择不同的语言
4. 确认整个界面立即切换到新语言
5. 关闭应用，重新打开
6. 确认应用使用之前选择的语言
7. 在中英文之间多次切换
8. 验证所有界面文本都支持双语

**验收标准**�?
- [x] 所有初始化场景正常
- [x] 语言切换流畅无卡�?
- [x] 用户语言偏好正确保存
- [x] 应用重启后保留语言选择
- [x] 无控制台错误和警�?
- [x] 中英文文本都正确显示

**工作�?*�? 小时

---

## 总工作量估计

| 阶段 | 任务 | 工作�?|
|------|------|--------|
| **i18n-core** | 1.1 | 0.5h |
| | 1.2 | 2h |
| | 1.3 | 1.5h |
| | 1.4 | 0.5h |
| | 1.5 | 2h |
| | 1.6 | 1.5h |
| | 1.7 | 1h |
| **小计** | | **9h** |
| **i18n-settings** | 2.1 | 3h |
| | 2.2 | 2h |
| | 2.3 | 1.5h |
| | 2.4 | 1h |
| | 2.5（测试） | 2h |
| **小计** | | **9.5h** |
| **总计** | | **18.5h** |

---

## 验证检查清�?

实施完成后的最终验收：

- [x] vue-i18n 已安装并配置
- [x] 翻译资源文件完整（中英文�?
- [x] 前端组件使用 i18n 正常显示
- [x] 后端能检测系统语言
- [x] 后端能保�?读取语言偏好
- [x] 前端能调用后端语言接口
- [x] Settings 页面可访�?
- [x] 语言选择下拉菜单工作正常
- [x] 实时语言切换有效
- [x] 语言偏好持久化保�?
- [x] 应用重启后保留语言选择
- [x] 无编译错误和警告
- [x] 功能测试全部通过
